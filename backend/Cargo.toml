[package]
name = "backend"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
axum = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sqlx = { version = "0.9", default-features = false, features = ["runtime-tokio", "sqlite", "chrono", "migrate", "macros"] }
chrono = { version = "0.4", features = ["serde"] }
btleplug = "0.12"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
thiserror = "2"
futures = "0.3"
uuid = "1"

# BlueZ adapter power control for the scanner's stall-recovery path
# (`ble::adapter_power`). Same version btleplug 0.12 already pins, so no
# extra copy. Linux only — CoreBluetooth exposes no adapter power control.
[target.'cfg(target_os = "linux")'.dependencies]
bluez-async = "0.8"

[dev-dependencies]
tower = { version = "0.5", features = ["util"] }
